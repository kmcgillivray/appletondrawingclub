# Frontend Environment Variables (Required for client-side)
# These are safe to expose in the browser and used by SvelteKit

# Your Supabase project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase publishable/anon key (safe for client-side use)
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Stripe publishable key for client-side payments
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Server-side Environment Variables (Keep secret!)
# These are used by SvelteKit server-side code and should never be exposed to the browser

# Supabase service role key (full database access - keep secret!)
SUPABASE_PRIVATE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Note: Edge Function environment variables are configured separately
# in the Supabase dashboard or supabase/functions/.env file:
# - RESEND_API_KEY
# - TRANSACTIONAL_FROM_EMAIL
# - STRIPE_SECRET_KEY
# - STRIPE_WEBHOOK_SECRET
# - SB_SECRET_KEY (same as SUPABASE_PRIVATE_KEY)