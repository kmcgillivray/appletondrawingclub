# Supabase Edge Functions Environment Variables
# These variables are configured in the Supabase dashboard under Edge Functions settings
# or can be set locally in this file for development

# Stripe Configuration (Payment Processing)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here

# Transactional Email Service (Resend)
RESEND_API_KEY=re_your_resend_api_key_here
TRANSACTIONAL_FROM_EMAIL=hello@appletondrawingclub.com

# Supabase Database Access (Server-side only)
# This should match SUPABASE_PRIVATE_KEY from your frontend .env.local
SB_SECRET_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_URL=https://your-project-id.supabase.co

# Notes:
# - These variables are only accessible to Edge Functions, not the frontend
# - In production, set these in the Supabase dashboard Edge Functions settings
# - For local development, copy this file to .env and fill in real values
# - NEVER commit the actual .env file with real secrets!